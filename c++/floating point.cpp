#include<iostream>
using namespace std;
int main()
{
  float a,b,result;
  cout<<"enter a "<<'\n';
  cin>>a;
  cout<<"enter b "<<'\n';
  cin>>b;
  
  result=a+b;
  cout<<"the sum of two numbers is "<<result<<'\n';
  
  result=a-b;
  cout<<"the difference of two numbers is "<<result<<'\n';
  
  result=a*b;
  cout<<"the product of two numbers is "<<result<<'\n';

  result=a/b;
  cout<<"the division of two numbers is "<<result<<'\n';
  
  
  return 0;
  
}
